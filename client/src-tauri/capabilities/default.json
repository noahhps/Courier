{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window is allowed to ask the shell to do.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
